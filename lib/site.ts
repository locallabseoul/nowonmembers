// 사이트의 공개 주소. 문자에 들어가는 링크는 반드시 이 주소여야 한다.
//
// NEXT_PUBLIC_SITE_URL은 개발 중에 localhost를 가리킨다. 문자는 운영자 PC에서 보내므로
// 그 값을 쓰면 회원에게 localhost 주소가 담긴 문자가 나간다. 그래서 공개 주소는 따로 둔다.
export const PUBLIC_SITE_URL = "https://nowon-me.kr";

// 광고성 정보에는 보내는 사람이 누구인지 밝혀야 한다. 회원이 가입한 이름으로 적는다.
export const SERVICE_NAME = "노원멤버스";

// 광고 문자에 적는 수신거부 경로. 문자 길이가 요금을 가르므로 짧게 유지한다.
//
// 알리고 웹 화면에서 고를 수 있는 080 무료거부 번호(0808707302)는 API 발송에는 쓸 수
// 없다("무료거부 번호는 API에 사용하실 수 없습니다"로 거부된다). 그래서 회원이 직접
// 끄는 화면으로 안내한다. API에서 쓸 수 있는 080 번호를 알리고에서 받으면 이 값 대신
// 그 번호를 적으면 된다.
export const OPT_OUT_PATH = "/optout";
